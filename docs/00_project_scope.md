# Project Scope

This project is a minimal Vision-Language-Action framework for learning robot action prediction.

V0 focuses on:

```text
image + instruction + state -> action
```

V0 does not include real robot control, large VLA training, diffusion policy, action tokenization, multi-camera input, depth input, LeRobot data collection, or complex UI.
