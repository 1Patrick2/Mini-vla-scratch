# Training Pipeline

Stage 3 will implement behavior cloning with:

```text
MSE(action_pred, action_gt)
```

The training entry point is `scripts/train.py`; the training logic belongs in `mini_vla/training/`.
