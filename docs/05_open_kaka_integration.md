# Open Kaka Integration

Open Kaka integration is future work.

No real robot should be controlled by default. Future implementations must go through explicit enable flags, action clipping, workspace or joint limits, speed limits, dry-run mode, and logging.
