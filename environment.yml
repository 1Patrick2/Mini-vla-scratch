name: mini-vla
channels:
  - pytorch
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pip
  - numpy
  - pillow
  - matplotlib
  - pyyaml
  - tqdm
  - pytest
  - pip:
      - torch
      - torchvision
      - rich
      - einops
      - opencv-python
